apiVersion: 1
providers:
  - name: ai-model-arena
    orgId: 1
    folder: ai-model-arena
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
