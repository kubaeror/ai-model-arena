apiVersion: apps/v1
kind: Deployment
metadata:
  name: runner-openai-compat
  namespace: ai-arena
  labels:
    app: runner
    provider: openai-compat
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: runner
      provider: openai-compat
  template:
    metadata:
      labels:
        app: runner
        provider: openai-compat
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: db-migrate
          image: ghcr.io/kubaeror/ai-model-arena-runner:PLACEHOLDER
          command: ["node", "dist/scripts/db-migrate.js"]
          envFrom:
            - configMapRef:
                name: runner-config
            - secretRef:
                name: arena-db-auth
          securityContext:
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
      containers:
        - name: runner
          image: ghcr.io/kubaeror/ai-model-arena-runner:PLACEHOLDER
          envFrom:
            - configMapRef:
                name: runner-config
            - secretRef:
                name: arena-db-auth
          env:
            - name: REDIS_CONSUMER_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: ARENA_PROVIDER_FILTER
              value: openai-compat
          ports:
            - name: metrics
              containerPort: 4001
          volumeMounts:
            - name: outputs
              mountPath: /var/arena/outputs
            - name: tmp
              mountPath: /tmp
            - name: readiness
              mountPath: /var/arena/readiness
            - name: provider-keys
              mountPath: /etc/arena/secrets
              readOnly: true
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          startupProbe:
            exec:
              command: ["test", "-f", "/var/arena/readiness/runner-ready"]
            failureThreshold: 30
            periodSeconds: 2
          livenessProbe:
            exec:
              command: ["test", "-f", "/var/arena/readiness/runner-ready"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["test", "-f", "/var/arena/readiness/runner-ready"]
            initialDelaySeconds: 5
            periodSeconds: 5
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: runner
              provider: openai-compat
      volumes:
        - name: outputs
          persistentVolumeClaim:
            claimName: outputs
        - name: tmp
          emptyDir: {}
        - name: readiness
          emptyDir: {}
        - name: provider-keys
          secret:
            secretName: provider-keys
