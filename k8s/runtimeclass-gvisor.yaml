apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
# Only created when gVisor is available (Linux minikube).
# On Windows minikube, omit runtimeClassName from pod specs.
